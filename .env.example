# Local-only core needs no credentials. Cloud sync is a later milestone.
# Never put a Supabase service-role key in frontend environment variables.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

